<button type="submit"
    id="{{ $id }}"
    name="{{ $name }}"
    class="bg-{{ $color }}-700 text-white active:bg-{{ $color }}-700 font-bold uppercase text-xs px-4 py-2 rounded shadow hover:shadow-md hover:bg-{{ $color }}-300 outline-none focus:outline-none mr-1 ease-linear transition-all duration-150 w-full">
    {{ __($title) }}
</button>
