<div class="flex flex-wrap items-center border-none border-teal-500 w-full  md:w-32-6per pt-5 md:pt-0">
    <label for="{{$id}}" class="block uppercase text-blueGray-600 text-xs font-bold mb-2">
        {{ __($title) }}</label>
    <select name="{{$name}}" id="{{$id}}" class="{{$class}} w-full" required>
    </select>
</div>
